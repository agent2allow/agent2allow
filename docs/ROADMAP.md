# Roadmap

1. Add idempotency keys for `/v1/tool-calls` to prevent duplicate writes.
2. Add retry/backoff behavior in GitHub connector with explicit tests.
3. Add policy schema validation CLI (`agent2allow policy validate`).
4. Add SDK quickstart snippets with approval examples (JS + Python).
5. Add denied-call troubleshooting guide with common fixes.
6. Add compact approval reason presets in UI.
7. Add audit event schema version field for future compatibility.
8. Add policy wizard CLI scaffold for common templates.
9. Expand integration tests for real-mode safety checks.
10. Add container health checks and startup readiness probes.
