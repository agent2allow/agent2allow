# Test Report: Iteration 3 (2026-02-17)

## Scope under test
- Idempotency support for `/v1/tool-calls` using `X-Idempotency-Key`.
- SDK examples for approval-aware and retry-safe usage.

## Commands
```bash
cd gateway && ruff check src tests
cd gateway && pytest -q
cd ui && npm run lint
cd ui && npm test --silent
```

## Results
- `ruff check`: pass
- `pytest -q`: pass
  - validates replay behavior for matching idempotency key
  - validates conflict response (`409`) for key reuse with different payload
- `ui` lint/tests: pass
- Docker smoke: not executed in local WSL because `docker` CLI is unavailable.
  - expected verification path: CI `compose-smoke` and `demo-smoke`
