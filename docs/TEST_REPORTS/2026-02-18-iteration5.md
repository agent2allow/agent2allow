# Test Report: Iteration 5 (2026-02-18)

## Scope under test
- Approval UI bulk actions and clearer status labels.
- Audit event schema versioning with startup-safe SQLite migration.

## Commands
```bash
cd gateway && ruff check src tests
cd gateway && pytest -q
cd ui && npm run lint
cd ui && npm test --silent
./scripts/dev_check.sh
```

## Results
- Gateway lint/tests: pass.
- UI lint/tests: pass.
- Integration verifies `schema_version=1` in `/v1/audit` and `/v1/audit/export`.
- Local `dev_check.sh`: pass (compose validation skipped in WSL if Docker unavailable there).
