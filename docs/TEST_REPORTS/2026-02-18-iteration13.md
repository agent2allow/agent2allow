# Test Report: Iteration 13 (2026-02-18)

## Scope under test
- Optional API key auth for approval mutation endpoints.
- Identity mapping from API key into RBAC/approval execution.
- SDK compatibility with approval API key header.

## Commands
```bash
cd gateway && ruff check src tests
cd gateway && pytest -q
./scripts/dev_check.sh
```

## Results
- Gateway lint/tests: pass.
- API key behavior validated:
  - missing key -> `401`
  - invalid key -> `403`
  - valid key -> approval execution succeeds
- Existing flows remain compatible when API key auth is disabled (default).
