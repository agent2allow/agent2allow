# Test Report — Iteration 1 (2026-02-17)

## Scope validated
- README/quickstart improvements
- mock demo smoke script integration
- triage template support + summary output
- audit viewer filters/details/export UX

## Local verification
1. Gateway lint + tests
- Command: `cd gateway && ruff check src tests && pytest -q`
- Result: pass

2. UI lint + tests + build
- Command: `cd ui && npm run lint && npm test --silent && npm run build`
- Result: pass

3. Demo flow (local mock)
- Services started via local uvicorn for gateway + mock github.
- Command: `npm run demo`
- Result: pass
- Verified signals:
  - deny-by-default blocked unscoped read
  - read executed
  - pending approvals created for writes
  - approvals executed
  - audit summary printed

## CI expectation
- `lint`, `gateway-tests`, `ui-tests`, `secret-scan`, `compose-smoke`, `demo-smoke` should run on PR.
